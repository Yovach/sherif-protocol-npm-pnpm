{
  "name": "sherif-protocol-npm-pnpm",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "packageManager": "npm@10.1.0",
  "workspaces": [
    "packages/*"
  ]
}
